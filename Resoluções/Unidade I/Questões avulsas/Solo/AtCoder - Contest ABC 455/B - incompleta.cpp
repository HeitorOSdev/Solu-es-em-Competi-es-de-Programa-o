#include <bits/stdc++.h>
using namespace std;
int main()
{
    ios::sync_with_stdio(0); cin.tie(0);
    
    int H,W, output = 0;
    cin >> H >> W;
    
    vector<string> v(H);
    
    
    for(int i = 0; i < H; i++){
        string s;
        getline(cin, s);
        v.push_back(s);
    }
    
    
    
    for(int i = 0; i<W; i++){
        for(int j=0; j<H; j++){
            
            if(v[i])
            
            if(v[]%2 == 0)
            
        }
    }
    
    
    
    
    return 0;
}